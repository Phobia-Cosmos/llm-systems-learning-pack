# MiniLLM HF-like Export

This directory is useful for learning the Hugging Face model layout. Tokenizer type: `byte-bpe`.

It can be loaded by the sibling nano-vLLM and vLLM MiniGPT backends. Upstream SGLang still needs its own MiniGPT implementation and registration.
