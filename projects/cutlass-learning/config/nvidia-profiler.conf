# Allow non-admin users to access NVIDIA GPU performance counters after reboot.
# This is appropriate only for a trusted, single-user development workstation.
options nvidia NVreg_RestrictProfilingToAdminUsers=0

